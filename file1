function isSorted(arr) {
  for (let i = 1; i < arr.length; i++) {
    if (arr[i] < arr[i - 1]) {
      return false;
    }
  }
  return true;
}

let numbers = [1, 2, 3, 5, 4];
console.log(isSorted(numbers));
